pipeline {
      agent any
          stages {

	        stage('Checkout GIT ') {
                 steps {
                    echo 'Pulliing ...';
		           
                        }
                 }
}
}
